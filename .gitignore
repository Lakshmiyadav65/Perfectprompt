# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets — never commit your actual .env
.env
.env.*
!.env.example

# Supabase CLI machine-local state (linked project ref, version pins,
# pooler URL). Not secret on its own but per-machine; the CLI rewrites
# these on every `supabase link` / `supabase db push`.
backend/supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
