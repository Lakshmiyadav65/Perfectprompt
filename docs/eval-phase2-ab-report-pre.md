# Phase 2 A/B context-bleed diagnostic â€” pass 1 (pre-fixes)

Generated by `cargo run --example eval_phase2_ab`. Model: `llama-3.3-70b-versatile`. 
Code state: **before** `reject_if_outsources_content` and the Mode B 
NEVER extension. This report is the baseline for the post-fix re-run.

**Active project:** `prompt` (id `proj_1778767636512`)  
**Project description length:** 1514 chars  
**Project path:** (none)  
**Cached GitHub fetch:** absent  
**`<context>` block length:** 1603 chars  
**`CONTEXT_THRESHOLD_BUMP`:** 0 (routing unchanged WITH vs WITHOUT)

## Context block used for WITH passes (verbatim)

```
<context>
Project: prompt
Stack: Rust, Tauri, React, TypeScript

Description:
--- README ---
# PerfectPrompt

> A system-tray prompt enhancer. Press a global hotkey anywhere on your computer to turn rough prompts into precise ones — works in any app where you can select text.

Built with Tauri 2 + Rust + React + TypeScript.

---

## What it does

1. Select rough prompt text in any app (Notepad, Brave, VS Code chat, terminal, anywhere).
2. Press the global hotkey (default `Ctrl+Alt+E` on Windows, `Cmd+Option+E` on macOS).
3. PerfectPrompt captures the selection and runs the **Smart Question Engine** (see below). The result is pasted back over your selection.

Think Wispr Flow, but for written prompts to coding agents (Claude Code, Cursor, ChatGPT) instead of voice.

### Two paths after the hotkey

PerfectPrompt scores the captured input for "enhancement ambiguity" and branches:

- **Silent fast-path** (`score < threshold`, or `question_mode = Silent`, or **`Shift`+hotkey**): one LLM call, a small "Enhancing…" pill near your cursor, the enhanced text replaces your selection in ~2–3 seconds.
- **Question-card path** (`score ≥ threshold`, or `question_mode = Always ask`): a 360px floating card appears with 2–4 chip questions about audience / tone / goal / constraints / format. Pick the most defensible answers (Enter submits, Escape skips, arrow keys navigate), and the enhanced prompt — informed by your answers — replaces your selection.

The card's questions are generated by a small Groq model (`llama-3.1-8b-instant`) in parallel with the window opening, with a h
[…truncated]
</context>
```

---

## Prompt 1 â€” Favorites feature (~1119 chars)

### WITHOUT context, run 1

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=1134 Â· raw-LLM=647 Â· final=647 (ratio 0.58)
- **Latency:** total 750ms Â· LLM 732ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement the "favorite prompts" feature by adding a star icon to each recent-history item, allowing users to mark and unmark favorites. Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible and limited to 50 favorites. Ensure that favorites persist across app restarts and sync to the trace log for eval reports. When a favorite is removed from the Favorites section, also unmark it as a favorite everywhere else. Maintain the existing recent-history behavior and visual style, and ensure that tests cover marking, unmarking, the 50-limit, persistence, and recent-history filtering.
```

---

### WITHOUT context, run 2

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=1134 Â· raw-LLM=1240 Â· final=1240 (ratio 1.11)
- **Latency:** total 946ms Â· LLM 945ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement the "favorite prompts" feature by adding the following functionality: 
Add a star icon to each item in the recent-history view that toggles favorite status when clicked, displaying a filled star for favorites and an outlined star for non-favorites. 
Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible. 
Limit the number of favorites to 50; when attempting to add a 51st favorite, display a tray notification. 
Ensure that removing a favorite from the Favorites section also removes the favorite status from the recent-history view. 
Persist favorites on disk alongside other settings, ensuring they are retained across app restarts. 
Sync favorites with the trace log to include them in eval reports. 
Maintain the existing recent-history behavior, only adding the favorite-marking functionality. 
Keep the feature local-only, without cloud sync, multi-device sync, or sharing capabilities, and ensure the visual style remains consistent with the existing sidebar. 
Develop tests to cover the following scenarios: marking and unmarking favorites, the 50-favorite limit, persistence across app restarts, and the continued functionality of recent-history filtering.
```

---

### WITHOUT context, run 3

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=1134 Â· raw-LLM=1105 Â· final=1105 (ratio 0.99)
- **Latency:** total 1045ms Â· LLM 1044ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement a "favorite prompts" feature with the following specifications: 
Add a star icon to each item in the recent-history view that toggles favorite status when clicked, displaying a filled star for favorites and an outlined star for non-favorites. 
Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible. 
Limit the number of favorites to 50; when attempting to add a 51st favorite, display a tray notification. 
Ensure that removing a favorite from the Favorites section also removes it from the favorite status everywhere. 
Persist favorites on disk alongside other settings, and sync them to the trace log for inclusion in eval reports. 
Maintain the existing recent-history behavior, with the addition of favorite-marking functionality. 
Conduct tests to cover the following scenarios: marking and unmarking favorites, the 50-favorite limit, persistence across app restarts, and the continued functionality of recent-history filtering. 
Ensure the visual style of the new features remains consistent with the existing sidebar design.
```

---

### WITH context, run 1

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=2739 Â· raw-LLM=690 Â· final=690 (ratio 0.62)
- **Latency:** total 959ms Â· LLM 958ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement the "favorite prompts" feature by adding a star icon to each recent-history item, allowing users to mark and unmark favorites. Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible. Ensure that favorites are limited to 50, with a tray notification displayed when attempting to add a 51st favorite. Implement persistence of favorites across app restarts, syncing with the trace log for eval reports, and maintain consistency with the existing sidebar visual style. Develop tests to cover marking and unmarking favorites, the 50-limit, persistence across restarts, and that recent-history filtering remains functional.
```

---

### WITH context, run 2

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=2739 Â· raw-LLM=749 Â· final=749 (ratio 0.67)
- **Latency:** total 707ms Â· LLM 706ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement the "favorite prompts" feature by adding a star icon to each recent-history item, allowing users to toggle favorite status. Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible. Ensure that favorites are limited to 50 maximum, and attempting to add a 51st favorite displays a tray notification. Implement persistence of favorites across app restarts, and sync favorites to the trace log for eval reports. Develop tests to cover marking/unmarking, the 50-limit, persistence, and that recent-history filtering remains functional. Maintain the existing visual style and recent-history behavior, and keep the feature local-only without cloud sync, multi-device sync, or sharing.
```

---

### WITH context, run 3

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=2739 Â· raw-LLM=767 Â· final=767 (ratio 0.69)
- **Latency:** total 649ms Â· LLM 648ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement a "favorite prompts" feature in the PerfectPrompt application. Add a star icon to each recent-history item, allowing users to toggle favorite status. Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible. Limit the number of favorites to 50, displaying a tray notification when this limit is exceeded. Ensure that removing a favorite from the Favorites section also unfavorites it everywhere, and that favorites persist on disk alongside other settings and sync to the trace log. Maintain the existing recent-history behavior and visual style, and develop tests to cover marking/unmarking, the 50-limit, persistence across restarts, and that recent-history filtering still functions as expected.
```

---

## Prompt 2 â€” Bug report (~1095 chars)

### WITHOUT context

- **Route:** `code` Â· domain `Coding` Â· ambiguity `25`
- **Lengths:** input=1095 Â· user-message=1081 Â· raw-LLM=381 Â· final=381 (ratio 0.35)
- **Latency:** total 991ms Â· LLM 989ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Rewrite the provided JavaScript function to use TypeScript with proper types, preserving the original code block. The function in question is the debounce function that takes a function and a timeout in milliseconds, and returns a debounced version of the input function. Add a regression test to ensure that code blocks within the input are preserved during the rewriting process.
```

---

### WITH context

- **Route:** `code` Â· domain `Coding` Â· ambiguity `25`
- **Lengths:** input=1095 Â· user-message=2686 Â· raw-LLM=524 Â· final=524 (ratio 0.48)
- **Latency:** total 6731ms Â· LLM 6730ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Investigate the bug where the enhancer drops code blocks from inputs. Identify the issue with Stage E's strip_code_fences validator and modify it to preserve code fences that are part of the user's original input. Add a regression test that covers the specific case of rewriting a prompt with a code block, ensuring that the code block is preserved in the output. Use the provided trace log in app_data_dir/traces/2025-05-14.jsonl to inform the fix, searching for the entry where validators_fired contains strip_code_fences.
```

---

## Length-ratio scorecard (Prompt 1 â€” 1400-char input threshold 0.33)

| Run | Input | Final | Ratio | Below 0.33? |
|---|---|---|---|---|
| WITHOUT context, run 1 | 1119 | 647 | 0.58 | no |
| WITHOUT context, run 2 | 1119 | 1240 | 1.11 | no |
| WITHOUT context, run 3 | 1119 | 1105 | 0.99 | no |
| WITH context, run 1 | 1119 | 690 | 0.62 | no |
| WITH context, run 2 | 1119 | 749 | 0.67 | no |
| WITH context, run 3 | 1119 | 767 | 0.69 | no |

