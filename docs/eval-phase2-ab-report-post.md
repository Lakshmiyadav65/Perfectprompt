# Phase 2 A/B context-bleed diagnostic — pass 2 (post-fixes)

Generated by `cargo run --example eval_phase2_ab`. Model: `llama-3.3-70b-versatile`. 
Code state: **after** the 4 Pass-2 fixes:
(1) tightened convention block in all 3 route prompts,
(2) extended Mode B NEVER to cover subsystem/feature names,
(3) `reject_if_outsources_content` validator shipped,
(4) Generic route temperature 0.4 → 0.3.
Pair the diff section at the end against `eval-phase2-ab-report-pre.md`.

**Active project:** `prompt` (id `proj_1778767636512`)  
**Project description length:** 1514 chars  
**Project path:** (none)  
**Cached GitHub fetch:** absent  
**`<context>` block length:** 1603 chars  
**`CONTEXT_THRESHOLD_BUMP`:** 0 (routing unchanged WITH vs WITHOUT)

## Context block used for WITH passes (verbatim)

```
<context>
Project: prompt
Stack: Rust, Tauri, React, TypeScript

Description:
--- README ---
# PerfectPrompt

> A system-tray prompt enhancer. Press a global hotkey anywhere on your computer to turn rough prompts into precise ones — works in any app where you can select text.

Built with Tauri 2 + Rust + React + TypeScript.

---

## What it does

1. Select rough prompt text in any app (Notepad, Brave, VS Code chat, terminal, anywhere).
2. Press the global hotkey (default `Ctrl+Alt+E` on Windows, `Cmd+Option+E` on macOS).
3. PerfectPrompt captures the selection and runs the **Smart Question Engine** (see below). The result is pasted back over your selection.

Think Wispr Flow, but for written prompts to coding agents (Claude Code, Cursor, ChatGPT) instead of voice.

### Two paths after the hotkey

PerfectPrompt scores the captured input for "enhancement ambiguity" and branches:

- **Silent fast-path** (`score < threshold`, or `question_mode = Silent`, or **`Shift`+hotkey**): one LLM call, a small "Enhancing…" pill near your cursor, the enhanced text replaces your selection in ~2–3 seconds.
- **Question-card path** (`score ≥ threshold`, or `question_mode = Always ask`): a 360px floating card appears with 2–4 chip questions about audience / tone / goal / constraints / format. Pick the most defensible answers (Enter submits, Escape skips, arrow keys navigate), and the enhanced prompt — informed by your answers — replaces your selection.

The card's questions are generated by a small Groq model (`llama-3.1-8b-instant`) in parallel with the window opening, with a h
[…truncated]
</context>
```

---

## Prompt 1 â€” Favorites feature (~1119 chars)

### Prompt 1 â€” WITHOUT context, run 1

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=1134 Â· raw-LLM=1144 Â· final=1144 (ratio 1.02)
- **Latency:** total 1036ms Â· LLM 1018ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement a "favorite prompts" feature with the following specifications: 
Add a star icon to each recent-history item, allowing users to toggle favorite status by clicking on it. 
The star icon should be filled when a prompt is marked as a favorite and outlined when not. 
Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, and make it collapsible. 
Limit the number of favorites to 50; when a user attempts to add a 51st favorite, display a tray notification. 
Ensure that removing a favorite from the "Favorites" section also unfavorites it in all other locations. 
Store favorites on disk alongside other settings to enable persistence across app restarts. 
Sync favorites with the trace log so they appear in evaluation reports. 
Preserve the existing recent-history behavior and only add the favorite-marking functionality on top. 
Maintain a consistent visual style with the existing sidebar. 
Develop tests to cover the following scenarios: marking and unmarking favorites, the 50-favorite limit, persistence across app restarts, and the continued functionality of recent-history filtering.
```

---

### Prompt 1 â€” WITHOUT context, run 2

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=1134 Â· raw-LLM=1117 Â· final=1117 (ratio 1.00)
- **Latency:** total 884ms Â· LLM 883ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement a "favorite prompts" feature with the following specifications: 
Add a star icon to each recent-history item, allowing users to toggle favorite status by clicking on it, with the star filled when the prompt is a favorite and outlined when not. 
Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible. 
Limit the number of favorites to 50; when a user attempts to add a 51st favorite, display a tray notification. 
Ensure that removing a favorite from the Favorites section also unfavorites it in all other locations. 
Persist favorites on disk alongside other settings, allowing them to survive app restarts. 
Sync favorites to the trace log so they appear in evaluation reports. 
Preserve the existing recent-history behavior, only adding the favorite-marking functionality on top. 
Maintain a consistent visual style with the existing sidebar. 
Develop tests to cover the following scenarios: marking and unmarking favorites, the 50-favorite limit, persistence across app restarts, and the continued functionality of recent-history filtering.
```

---

### Prompt 1 â€” WITHOUT context, run 3

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=1134 Â· raw-LLM=1128 Â· final=1128 (ratio 1.01)
- **Latency:** total 923ms Â· LLM 922ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement a "favorite prompts" feature with the following specifications: 
Add a star icon to each recent-history item, allowing users to toggle favorite status by clicking on it, with the star filled when the prompt is a favorite and outlined when not. 
Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible. 
Limit the number of favorites to 50; when attempting to add a 51st favorite, display a tray notification. 
Ensure that removing a favorite from the Favorites section also unfavorites it everywhere. 
Persist favorites on disk alongside other settings, and sync them to the trace log for inclusion in eval reports. 
Maintain the existing recent-history behavior, only adding the favorite-marking feature on top, and preserve the visual style consistent with the existing sidebar. 
Do not implement cloud sync, multi-device sync, or sharing; favorites should be local only. 
Develop tests to cover marking and unmarking favorites, the 50-favorite limit, persistence across app restarts, and verify that recent-history filtering still functions as expected.
```

---

### Prompt 1 â€” WITH context, run 1

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=2739 Â· raw-LLM=735 Â· final=735 (ratio 0.66)
- **Latency:** total 1995ms Â· LLM 1994ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement a "favorite prompts" feature in the PerfectPrompt application. Add a star icon to each recent-history item, allowing users to toggle favorite status. Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible and limited to 50 favorites. Ensure that favorites persist across app restarts, are stored on disk alongside other settings, and are synced to the trace log for eval reports. Develop tests to cover marking and unmarking favorites, the 50-favorite limit, persistence across restarts, and verify that recent-history filtering remains functional. Maintain a consistent visual style with the existing sidebar and do not alter the existing recent-history behavior.
```

---

### Prompt 1 â€” WITH context, run 2

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=2739 Â· raw-LLM=984 Â· final=984 (ratio 0.88)
- **Latency:** total 944ms Â· LLM 943ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement a "favorite prompts" feature by adding the following functionality: 
In the recent-history view, add a star icon to each item that toggles favorite status when clicked, displaying a filled star for favorites and an outlined star for non-favorites. 
Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible. 
Limit the number of favorites to 50; when a user attempts to add a 51st favorite, display a tray notification. 
Ensure that removing a favorite from the Favorites section also removes it from the favorite status everywhere. 
Persist favorites on disk alongside other settings, and sync them to the trace log for inclusion in eval reports. 
Maintain the existing recent-history behavior and visual style consistent with the sidebar. 
Develop tests to cover marking and unmarking favorites, the 50-favorite limit, persistence across app restarts, and verify that recent-history filtering remains functional.
```

---

### Prompt 1 â€” WITH context, run 3

- **Route:** `generic` Â· domain `Generic` Â· ambiguity `25`
- **Lengths:** input=1119 Â· user-message=2739 Â· raw-LLM=765 Â· final=765 (ratio 0.68)
- **Latency:** total 739ms Â· LLM 738ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Implement a "favorite prompts" feature in the PerfectPrompt application. Add a star icon to each recent-history item, allowing users to toggle favorite status. Create a new "Favorites" section in the sidebar, positioned between "Recent" and the usage card, which is collapsible. Limit the number of favorites to 50, displaying a tray notification when this limit is exceeded. Ensure that removing a favorite from the Favorites section also unfavorites it everywhere. Persist favorites on disk alongside other settings and sync them to the trace log for eval reports. Maintain the existing recent-history behavior and visual style, and ensure that tests cover marking/unmarking, the 50-limit, persistence across restarts, and that recent-history filtering still works.
```

---

## Prompt 1.5 — Refactor enhance.rs (Code-routed) (~890 chars)

### WITHOUT context, run 1

- **Route:** `code` Â· domain `Coding` Â· ambiguity `25`
- **Lengths:** input=890 Â· user-message=904 Â· raw-LLM=739 Â· final=739 (ratio 0.83)
- **Latency:** total 749ms Â· LLM 747ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Refactor the enhance.rs module to separate the Groq HTTP client logic into its own module, while keeping the route-specific enhancement logic intact. Name the new module for HTTP client logic as 'groq_client' and the existing enhancement logic module as 'enhance'. Move the HTTP client logic, including request building, response parsing, error handling, and API key reading from environment variables, to the 'groq_client' module. Ensure the 'enhance' module retains the same function name and parameters to maintain compatibility with pipeline.rs. Preserve the question-card flow and keep existing tests passing. Add new tests to cover the refactored code. Keep the implementation simple and avoid introducing a config struct or builder.
```

---

### WITHOUT context, run 2

- **Route:** `code` Â· domain `Coding` Â· ambiguity `25`
- **Lengths:** input=890 Â· user-message=904 Â· raw-LLM=651 Â· final=651 (ratio 0.73)
- **Latency:** total 6469ms Â· LLM 6468ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Refactor the enhance.rs module into two separate modules. Move the Groq HTTP client logic, including request building, response parsing, error handling, and API key reading, to a new module, e.g., groq_client.rs. Keep the route-specific enhancement logic, including loading the right system prompt per route and building the user message with <input> wrapping, in a new module, e.g., route_enhancer.rs, and ensure it remains callable from pipeline.rs with the same function name and parameters. Preserve the existing tests and add new tests to cover the split functionality. Ensure the Groq API key is still read from environment variables at startup.
```

---

### WITHOUT context, run 3

- **Route:** `code` Â· domain `Coding` Â· ambiguity `25`
- **Lengths:** input=890 Â· user-message=904 Â· raw-LLM=812 Â· final=812 (ratio 0.91)
- **Latency:** total 7242ms Â· LLM 7241ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Refactor the enhance.rs module to separate the Groq HTTP client logic into its own module, such as `groq_client.rs`, which will handle request building, response parsing, error handling, and API key reading from environment variables. 

Create another module, such as `enhance_logic.rs`, to hold the route-specific enhancement logic, including loading the right system prompt per route and building the user message with input wrapping. 

Ensure the function currently called by `pipeline.rs` remains unchanged in name and parameters, and that the question-card flow remains unaffected. 

Update existing tests to accommodate the changes and add new tests to cover the separated logic. 

Keep the API key retrieval simple, using environment variables at startup, and avoid introducing a config struct or builder.
```

---

### WITH context, run 1

- **Route:** `code` Â· domain `Coding` Â· ambiguity `25`
- **Lengths:** input=890 Â· user-message=2509 Â· raw-LLM=686 Â· final=686 (ratio 0.77)
- **Latency:** total 10230ms Â· LLM 10229ms
- **Validators fired:** Repaired (clean, no repair needed)

**Final output:**

```
Refactor the enhance.rs module to split its Groq HTTP client logic out of the route-specific enhancement logic. Move the HTTP client logic, including request building, response parsing, error handling, and API key reading, to a new module, e.g., groq_client.rs. The route-specific enhancement logic, including loading the right system prompt per route and building the user message with input wrapping, should remain in a new module, e.g., enhance_logic.rs, where it can still be called by pipeline.rs without changing the existing call signature. Ensure the existing tests pass and add new tests to cover the split logic. Keep the API key reading from environment variables at startup.
```

---

### WITH context, run 2

- **Route:** `code` Â· domain `Coding` Â· ambiguity `25`
- **Lengths:** input=890 Â· user-message=2509 Â· raw-LLM=0 Â· final=890 (ratio 1.00)
- **Latency:** total 92118ms Â· LLM 92117ms
- **Validators fired:** Error: llm_error: HTTP 429 Too Many Requests: {"error":{"message":"Rate limit reached for model `llama-3.3-70b-versatile` in organization `org_01kret3kcce0rsj8fx3b2g7hbj` service tier `on_demand` on tokens per day (TPD): Limit 100000, Used 99412, Requested 1867. Please try again in 18m25.056s. Need more tokens? Upgrade to Dev Tier today at https://console.groq.com/settings/billing","type":"tokens","code":"rate_limit_exceeded"}}


**Final output:**

```
Refactor the enhance.rs module to split its Groq HTTP client logic out of the route-specific enhancement logic. The HTTP client (request building, response parsing, error handling, API key reading) should move to its own module. The route-specific enhancement (loading the right system prompt per route, building the user message with <input> wrapping) should stay where pipeline.rs can call it.

Constraints:
- Don't change pipeline.rs's call signature. The function it currently calls should still exist with the same name and parameters.
- Don't change the question-card flow. Question generation has its own prompt and request shape; leave it alone.
- Keep existing tests passing. Add new tests for the split.
- Don't introduce a config struct or builder. Keep simple.

The Groq API key still comes from env vars at startup.

Suggest names for the two new modules and what goes in each.
```

---

### WITH context, run 3

- **Route:** `code` Â· domain `Coding` Â· ambiguity `25`
- **Lengths:** input=890 Â· user-message=2509 Â· raw-LLM=0 Â· final=890 (ratio 1.00)
- **Latency:** total 92245ms Â· LLM 92244ms
- **Validators fired:** Error: llm_error: HTTP 429 Too Many Requests: {"error":{"message":"Rate limit reached for model `llama-3.3-70b-versatile` in organization `org_01kret3kcce0rsj8fx3b2g7hbj` service tier `on_demand` on tokens per day (TPD): Limit 100000, Used 99305, Requested 1924. Please try again in 17m41.856s. Need more tokens? Upgrade to Dev Tier today at https://console.groq.com/settings/billing","type":"tokens","code":"rate_limit_exceeded"}}


**Final output:**

```
Refactor the enhance.rs module to split its Groq HTTP client logic out of the route-specific enhancement logic. The HTTP client (request building, response parsing, error handling, API key reading) should move to its own module. The route-specific enhancement (loading the right system prompt per route, building the user message with <input> wrapping) should stay where pipeline.rs can call it.

Constraints:
- Don't change pipeline.rs's call signature. The function it currently calls should still exist with the same name and parameters.
- Don't change the question-card flow. Question generation has its own prompt and request shape; leave it alone.
- Keep existing tests passing. Add new tests for the split.
- Don't introduce a config struct or builder. Keep simple.

The Groq API key still comes from env vars at startup.

Suggest names for the two new modules and what goes in each.
```

---

## Prompt 2 â€” Bug report (~1095 chars)

### Prompt 2 â€” WITHOUT context

- **Route:** `code` Â· domain `Coding` Â· ambiguity `25`
- **Lengths:** input=1095 Â· user-message=1081 Â· raw-LLM=0 Â· final=1095 (ratio 1.00)
- **Latency:** total 92018ms Â· LLM 92017ms
- **Validators fired:** Error: llm_error: HTTP 429 Too Many Requests: {"error":{"message":"Rate limit reached for model `llama-3.3-70b-versatile` in organization `org_01kret3kcce0rsj8fx3b2g7hbj` service tier `on_demand` on tokens per day (TPD): Limit 100000, Used 99199, Requested 1592. Please try again in 11m23.424s. Need more tokens? Upgrade to Dev Tier today at https://console.groq.com/settings/billing","type":"tokens","code":"rate_limit_exceeded"}}


**Final output:**

```
Filing a bug. The enhancer is dropping output on inputs that contain code blocks.

Repro: select this text in Notepad and hit Ctrl+Alt+E:

  Here's my function:
  
```javascript
  function debounce(fn, ms) {
    let t;
    return (...args) => {
      clearTimeout(t);
      t = setTimeout(() => fn(...args), ms);
    };
  }
```
  
  Rewrite this to use TypeScript with proper types.

Expected: the enhancer rewrites the request and preserves the code block.

Actual: the code block disappears from the output. The rewrite says "Rewrite the function above" but "the function above" is gone because the code block was stripped.

I suspect Stage E's strip_code_fences validator is being too aggressive â€” it's stripping fences from inside the input as part of repair instead of only stripping fences the model wrapped around its output. Trace log for this run is in app_data_dir/traces/2025-05-14.jsonl, search for the entry where validators_fired contains strip_code_fences.

Fix should preserve fences that are clearly part of the user's input. Add a regression test that covers this exact case.
```

---

### Prompt 2 â€” WITH context

- **Route:** `code` Â· domain `Coding` Â· ambiguity `25`
- **Lengths:** input=1095 Â· user-message=2686 Â· raw-LLM=0 Â· final=1095 (ratio 1.00)
- **Latency:** total 95197ms Â· LLM 95196ms
- **Validators fired:** Error: llm_error: HTTP 429 Too Many Requests: {"error":{"message":"Rate limit reached for model `llama-3.3-70b-versatile` in organization `org_01kret3kcce0rsj8fx3b2g7hbj` service tier `on_demand` on tokens per day (TPD): Limit 100000, Used 99092, Requested 1981. Please try again in 15m27.072s. Need more tokens? Upgrade to Dev Tier today at https://console.groq.com/settings/billing","type":"tokens","code":"rate_limit_exceeded"}}


**Final output:**

```
Filing a bug. The enhancer is dropping output on inputs that contain code blocks.

Repro: select this text in Notepad and hit Ctrl+Alt+E:

  Here's my function:
  
```javascript
  function debounce(fn, ms) {
    let t;
    return (...args) => {
      clearTimeout(t);
      t = setTimeout(() => fn(...args), ms);
    };
  }
```
  
  Rewrite this to use TypeScript with proper types.

Expected: the enhancer rewrites the request and preserves the code block.

Actual: the code block disappears from the output. The rewrite says "Rewrite the function above" but "the function above" is gone because the code block was stripped.

I suspect Stage E's strip_code_fences validator is being too aggressive â€” it's stripping fences from inside the input as part of repair instead of only stripping fences the model wrapped around its output. Trace log for this run is in app_data_dir/traces/2025-05-14.jsonl, search for the entry where validators_fired contains strip_code_fences.

Fix should preserve fences that are clearly part of the user's input. Add a regression test that covers this exact case.
```

---

## Length-ratio scorecard (Prompt 1 — favorites, threshold 0.33)

| Run | Input | Final | Ratio | Below 0.33? |
|---|---|---|---|---|
| â€” WITHOUT context, run 1 | 1119 | 1144 | 1.02 | no |
| â€” WITHOUT context, run 2 | 1119 | 1117 | 1.00 | no |
| â€” WITHOUT context, run 3 | 1119 | 1128 | 1.01 | no |
| â€” WITH context, run 1 | 1119 | 735 | 0.66 | no |
| â€” WITH context, run 2 | 1119 | 984 | 0.88 | no |
| â€” WITH context, run 3 | 1119 | 765 | 0.68 | no |

## Length-ratio scorecard (Prompt 1.5 — refactor enhance.rs, threshold 0.33)

| Run | Input | Final | Ratio | Below 0.33? |
|---|---|---|---|---|
| WITHOUT context, run 1 | 890 | 739 | 0.83 | no |
| WITHOUT context, run 2 | 890 | 651 | 0.73 | no |
| WITHOUT context, run 3 | 890 | 812 | 0.91 | no |
| WITH context, run 1 | 890 | 686 | 0.77 | no |
| WITH context, run 2 | 890 | 890 | 1.00 | no |
| WITH context, run 3 | 890 | 890 | 1.00 | no |

## Length-ratio scorecard (Prompt 2 — bug report, threshold 0.33)

| Run | Input | Final | Ratio | Below 0.33? |
|---|---|---|---|---|
|  â€” WITHOUT context | 1095 | 1095 | 1.00 | no |
|  â€” WITH context | 1095 | 1095 | 1.00 | no |

## Diff vs pass-1 baseline

Pass-1 values were captured from the pre-fix run's console output (see `eval-phase2-ab-report-pre.md`). Pass-2 values are computed live from this run.

| Metric | Pre (pass 1) | Post (pass 2) | Δ |
|---|---:|---:|---:|
| Prompt 1 WITHOUT avg ratio | 0.71 | 1.01 | +0.30 |
| Prompt 1 WITHOUT char-range (max−min) | 593 | 27 | -566 |
| Prompt 1 WITH avg ratio | 0.53 | 0.74 | +0.21 |
| Prompt 1.5 route used | n/a (not run pre) | `code` | (new) |
| Prompt 2 WITHOUT ratio | 0.34 | 1.00 | +0.66 |
| Prompt 2 WITH ratio | 0.47 | 1.00 | +0.53 |
| Validator firings (rejections, any of 14) | 0 | 0 | +0 |
| "Smart Question Engine" leaks across WITH outputs | 0 | 0 | +0 |
| Other subsystem-name leaks (PerfectPrompt / Wispr Flow) | 0 | 2 | +2 |

### Mode A signal — Prompt 1.5 WITH outputs (the Code-routed test case)

Counted across the 3 WITH-context Prompt 1.5 outputs:

- Rust-idiom hits (`Result<`, `Result::`, `match `, ` ?;`, ` Tauri `, `invoke handler`): **0**
- Softening hits (` consider `, ` could `, ` might `, `try/catch`, ` Promise`, ` async/await `): **0**

Verdict: Mode A is NOT firing — zero Rust idioms in Code-routed WITH outputs.

### Prompt 1.5 raw numbers (new in pass 2 — no pre-baseline to diff)

- WITHOUT avg ratio: 0.82 (lengths [739, 651, 812])
- WITH avg ratio: 0.92 (lengths [686, 890, 890])
- WITHOUT char-range (variance): 161
- WITH char-range (variance): 204

